# 📘 บันทึกการเรียนรู้: SOLID Principles

การเรียนรู้ในวันนี้เน้นไปที่หลักการออกแบบซอฟต์แวร์ที่ดี (Clean Object-Oriented Design) ซึ่งบัญญัติโดย **Robert C. Martin ("Uncle Bob")** เพื่อให้โค้ดมีความยืดหยุ่น อ่านง่าย และบำรุงรักษาได้ในระยะยาว

---

## 🏗️ S.O.L.I.D. คืออะไร?

| ตัวย่อ | หลักการ (Principle) | คำนิยามโดยย่อ |
| :---: | :--- | :--- |
| **S** | **Single Responsibility (SRP)** | หนึ่งคลาสควรมีเพียงหน้าที่เดียว และมีเหตุผลเดียวในการเปลี่ยนแปลง |
| **O** | **Open/Closed (OCP)** | เปิดรับการขยาย (Extension) แต่ปิดกั้นการแก้ไขโค้ดเดิม (Modification) |
| **L** | **Liskov Substitution (LSP)** | คลาสลูกต้องสามารถแทนที่คลาสแม่ได้ โดยไม่ทำให้โปรแกรมทำงานผิดเพี้ยน |
| **I** | **Interface Segregation (ISP)** | ไม่ควรบังคับให้ Client ต้องใช้งาน Interface หรือ Method ที่พวกเขาไม่ได้ใช้ |
| **D** | **Dependency Inversion (DIP)** | โมดูลระดับสูงไม่ควรขึ้นกับระดับต่ำ แต่ทั้งคู่ควรขึ้นอยู่กับ Abstraction |

---

## 🔍 เจาะลึก: Liskov Substitution Principle (LSP)

วันนี้เราได้ศึกษาปัญหาคลาสสิกของ **Square-Rectangle Problem** ซึ่งเป็นตัวอย่างที่ชัดเจนที่สุดของการละเมิดหลักการ LSP

### ❌ Problem (Bad Idea)
การให้ `Square` สืบทอดจาก `Rectangle` ดูเหมือนจะถูกตามหลักคณิตศาสตร์ แต่ในเชิง OOP มันสร้างผลข้างเคียง (Side Effect):
- เมื่อเราเปลี่ยนความกว้าง (`set_width`) ของ Square ความสูงก็จะเปลี่ยนตามอัตโนมัติ
- ฟังก์ชันที่รับ `Rectangle` ไปใช้งานจะเกิดความสับสน เพราะผลลัพธ์พื้นที่ (Area) จะไม่เป็นไปตามตรรกะของสี่เหลี่ยมผืนผ้าทั่วไป

### ✅ Solution (Good Idea)
แก้ไขโดยการใช้ **Abstraction**:
1. สร้าง Class `Shape` เป็นตัวแม่ (Abstract Base Class)
2. ให้ `Rectangle` และ `Square` แยกกันเป็นคนละ Class แต่สืบทอดจาก `Shape` เหมือนกัน
3. ใช้การตรวจสอบประเภท (`isinstance`) หรือการทำ **Polymorphism** เพื่อจัดการขนาดตามความเหมาะสมของแต่ละรูปทรง



---

## 🛠️ เครื่องมือที่ใช้ในวันนี้
- **Python**: ภาษาหลักในการเขียนโปรแกรม
- **uv**: เครื่องมือจัดการโปรเจกต์และ Environment ที่รวดเร็ว (`uv init`, `uv run`)
- **ABC (Abstract Base Classes)**: ใช้สร้าง Interface สำหรับโครงสร้างคลาสที่ถูกต้อง

---
*บันทึกเมื่อ: กุมภาพันธ์ 2026*